#include<iostream>
using namespace std;
int main()
{
    int x = 15;
    int y = 2;
    double z = x/y;//The division operator will output an integer as its answer, as its divident and the divisor are both integers.
    cout<<z;
    return 0;
}